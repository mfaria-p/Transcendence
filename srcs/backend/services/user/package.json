{
  "name": "@svc/user",
  "version": "1.0.0",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "test": "npx vitest",
    "typecheck": "tsc --noEmit --watch src/server.ts",
    "prisma:generate": "npx prisma generate --schema prisma/schema.prisma",
    "prisma:migrate": "npx prisma migrate dev --schema prisma/schema.prisma",
    "build": "tsc -p tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "prisma": "^6.17.0"
  },
  "dependencies": {
    "@fastify/error": "^4.2.0",
    "@pkg/lib-auth": "workspace:*",
    "@pkg/lib-docs": "workspace:*",
    "@prisma/client": "^6.17.0",
    "dotenv": "^17.2.3",
    "fastify": "^5.6.1",
    "fastify-plugin": "^5.1.0"
  }
}
